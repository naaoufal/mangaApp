import {
  Dimensions,
  FlatList,
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  TextInput,
  TouchableOpacity,
  useColorScheme,
  View,
} from 'react-native';

const GlobalContentScreen = () => {
  return (
    <View>
      <Text>this is global content screen</Text>
      <Text>list all mangas</Text>
    </View>
  );
};

export default GlobalContentScreen;
